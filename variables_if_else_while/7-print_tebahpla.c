#include <stdio.h>
/**
 * main - fonction principale
 *
 * exercice sur les conditions
 * Return: retourne un zero
 */
int main(void)
{
	int c;
	int n;

	c = '\n';
	for (n = 122; n > 96; n--)
	putchar(n);
	putchar(c);
	return (0);
}
